<?xml version="1.0"?>

<fragment version="1">
    <!-- Unzip the web resources into the server -->
    <install>
        <unzip from="${bundle.fileName}" to="/">
            <include>nuxeo.war/**</include>
        </unzip>
    </install>
</fragment>